const styles = {
    wrapper: {
        layout: "absolute -top-1 -right-2 flex flex-col items-center z-20",
        spacing: "px-2 p-1",
        border: "rounded-2xl",
        typo: "font-medium tracking-tight",
    },
};

export default styles;
