const styles = {
    wrapper: {
        layout: "relative",
    },
    error: {
        typo: "text-kborange text-center",
        sizing: "w-full",
        layout: "absolute bottom-14",
    },
};

export default styles;
